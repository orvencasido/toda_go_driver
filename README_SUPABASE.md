Run the shared SQL in `../supabase/schema.sql` in Supabase SQL Editor.

Driver accounts are created from the admin dashboard only. Start Flutter with:

```bash
flutter run --dart-define=SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co --dart-define=SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
```
